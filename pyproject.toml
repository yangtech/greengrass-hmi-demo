[project]
name = "greengrass-bedrock-ipc-demo"
version = "1.0.0"
description = "AWS IoT Greengrass v2 demo: Bedrock + IPC local pub/sub word-count loop"
requires-python = ">=3.9"

[project.optional-dependencies]
test = [
    "flask==3.1.3",
    "boto3==1.43.69",
]
